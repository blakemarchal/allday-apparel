// Character store landing at allday-apparel.com/WillAllday. Same view as the
// apparel root; getCurrentStorefront() returns the character store because the
// path is under /WillAllday.
export { default } from '@/components/storefront/views/StorefrontLanding';
