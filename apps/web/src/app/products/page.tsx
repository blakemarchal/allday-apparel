// Apparel catalog. Shares the CatalogPage view with /WillAllday/products;
// the active storefront is resolved from host + path inside the view.
export { default } from '@/components/storefront/views/CatalogPage';
